import { useRegisterActions } from 'kbar';

const useThemeSwitching = () => {
  // Theme switching functionality removed
  // Component kept for compatibility but no actions registered
};

export default useThemeSwitching;
